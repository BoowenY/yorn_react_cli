import <%= data.name%> from '../<%= data.name%>'
const <%= data.name%>Route = {
    path: "/<%= data.lowerName%>",
    exact: true,
    component: <%= data.name%>
}
export default <%= data.name%>Route